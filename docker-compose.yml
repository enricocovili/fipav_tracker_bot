services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: asteAI_telebot
    environment:
      - API_ID=${API_ID}
      - API_HASH=${API_HASH}
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
    restart: unless-stopped